# AI연구를 위한 배경지식을 공부한다.

## 목차
✅ CH1-1 함수와 다변수 함수
✅ CH1-2 로그 함수
✅ CH1-3 벡터와 행렬
✅ CH1-4 전치와 내적
✅ CH1-5 극한과 ε-δ 논법
✅ CH1-6 미분과 도함수
✅ CH1-7 연쇄 법칙(샤픈)
✅ CH1-8 편미분과 그라디언트
✅ CH1-9 테일러 급수
✅ CH1-10 스칼라를 벡터로 미분하는 법
✅ CH1-11 왜 그라디언트는 가장 가파른 방향을 향할까
✅ CH1-12 벡터를 벡터로 미분하는 법
✅ CH1-13 벡터를 벡터로 미분할때의 연쇄 법칙
✅ CH1-16 랜덤 변수와 확률 분포
✅ CH1-17 평균과 분산
✅ CH1-18 균등 분포와 정규 분포
✅ CH1-19 최대 우도 추정(MLE)
✅ CH1-20 최대 사후 확률(MAP)
✅ CH1-21 정보 이론 기초(Entropy, Cross Entropy, KL-divergence)



## 📚26.08.05 오늘의 TIL

### 1. 함수와 다변수 함수
 · 함수 및 다변수 함수가 필요한 이유
  - Deep learning에서는 학습을 통해 모델을 학습하며 발전해 나감.
    따라서 모델은 함수의 집합체이며, 실제 상황에 들어오는 값인 raw data는 x, y 등 변수로 정의할 수 있고 이런 변수가 들어왔을 때 최적의 해를 도출하여 예측하는것이 Deep learning의 목표이므로 함수가 필요함.
  - 신경망은 하나의 거대한 함수임.
    예를들어 y=f(x;θ) 여기서 x : 입력(Image) θ : Weight y : Output 이 되는데, 
    CNN에서는 Conv → ReLU → Pooling → FC 각각이 함수가 됨 즉, y=f4(f3(f2(f1(x)))) 처럼 함수를 계속 합성한 것임.
    그래서 "Deep Learning은 함수들의 합성(Composition of Functions)이다."

### 2. 로그
 · Deep learning에서 로그가 중요하게 쓰이는 이유
  - 로그함수는 지수함수의 역함수 이며 확률을 다루는데 효과적임 또한, 로그는 곱셈을 덧셈으로 바꿔 계산을 단순화하고, 작은 확률값을   안정적으로 다루게 해줌
  - 대표적으로 Cross-entropy, Log-likehood와 같은 손실 함수에서 모델의 예측과 실제값의 차이를 측정함
  - * 참고로 딥러닝에서 log에 자연상수 'e'가 많이 쓰이는데 이는 1. 미분이 간단하고(=>역전파가 간단해짐) 2. 연속적인 변화와 확률계산이 최적화 되어있기 때문 (ex) 확률 0.0000000003 과 같은 값을 그대로 곱하면 값이 계속 작아져 연산에 자원을 많이 소모하게 되는데, log를 취하면 -22 정도가 됨. 즉 곱셈 P(A)P(B)P(C) → logP(A)+logP(B)+logP(C) 가 된다. 따라서 수치적으로 매우 안정적이게 됨

### 3. 벡터와 행렬
 · 신경망에서 행렬 사용하는 이유
  - GPU가 행렬곱을 엄청 빠르게 계산할 수 있고, 실제 raw data는 나이/키/몸무게/주소 등 여러 특징(feature)을 갖게 됨

 · 신경망에서 스칼라뿐이 아니라 벡터를 사용하는 이유
  - 행렬에서 여러 입력 특성을 동시에 처리하고, 이러한 입력 특성은 여러 값(특성)으로 구성되어 있기 때문에 신경망에서는 각 특성을 벡터값으로 받아들이고 연산하여 다양한 문제를 해결하기 위해 벡터를 사용함

### 4. 전치와 내적(Dot Product)
 · 전치를 사용하는 이유
  - 행렬의 계산(내적)을 위해 차원을 맞추거나 벡터의 방향을 바꿔 수식의 전개와 계산을 단순화 하기 위해 사용
  - (8.10.추가) 추후 행렬이나 벡터를 미분할 때 계산이 자연스럽게 이어지도록(곱 방향이 맞도록) 방향을 맞추기 위한 수학적 약속을 함
  * 전치(^T)는 미분 결과의 행과 열(방향)을 곱셈이 자연스럽게 이어지도록 맞춰주는 역할
 · 내적의 계산 방법
  - 내적 = 유사도(Similarity) (ex) 고양이 특징[귀,수염,눈] - 강아지 특징[귀,코,눈] => 내적이 크다=비슷
  - [a1, a2]·[b1 b2] = a1b1 + a2b2 = a^t·b 와 같이 각각 곱하여 더하는 것이 내적
  - 또한, 내적은 닮은 정도를 나타내며 a^t·b는 ||a||·||b||cosθ 와 같음

### 5. 극한과 입실론-델타 논법
 · 극한의 개념
  - x가 a와 가까운 값일 때, f(x)는 어떤값과 가까운 값인지 에 대한 문제

 · ε-δ 증명
  - lim(x→a)f(x)=L 일때, L 주변의 갭으로 어떤 양수 ε을 잡더라도 그 갭으로 모두 보낼 수 있는 a주변의 δ갭이 존재한다면 a에서의 극한값은 L이라는 극한의 증명

### 6. 미분과 도함수
 · Deep learning에서 미분이 중요한 이유
   - 1차 함수에서는 순간기울기가 일정하지만, 다차함수에서는 각 시점마다의 기울기가 계속 변하므로, 순간기울기를 구하기 위해 미분이 핵심 개념으로 쓰임.

 · 도함수의 역할
   - 도함수는 함수의 변화율(입력이 변할 때 출력값의 변화)이 얼마나 변하는지를 쉽게 알려줌.
   - 결국 미분은 민감도(Sensitivity) 에 가까운데, (ex) 이미지를 0.001바꿧는데 출력값이 튄다면 민감하다는 뜻이고 이는 곧 Gradient가 크다라고 볼 수 있음.

새롭게 알게 된 것
 - 1. 신경망에서 로그에 자연상수 'e'가 자주쓰인다는 점 2. 'e'가 자주쓰이는 이유가 미분이 간단 / 역전파 용이 / 연속적 변화, 확률계산에 최적화 되어 있다는 점.

이해 안 되는 것
 - 벡터의 내적 부분이 정확하게 이해가 잘 되지는 않음.
 - ε-δ 증명 또한 대략적인 개념은 받아들이나, 증명과정 자체가 정확하게 이해되지 않음.
 - 금일의 학습 내용이 확실하게 추후 모델에 어느 부분에서 쓰이는지 대략적으로는 예상되지만 정확이 어디서 접목되는지 유기적인 관계는 아직 막연함.

논문과 연결될 것 같은 부분
 - Deep Learning은 모델의 알고리즘 자체가 수식으로 표현되기에 오늘 배운 모든 내용은 수식을 이해하고 생성함에 있어 연결이 될 것으로 생각함.


## 📚26.08.06 오늘의 TIL

### 7. 연쇄법칙(샤픈)
   - 함수의 순간기울기를 알고싶을때 미분이 필요한데, 합성함수(composite function)의 미분을 구하는 방법론. 
   연쇄법칙 증명을 통해 정형화된 미분법을 통하여 이항함수 이상의 함수식에서 보다 빠르게 미분값을 구할 수 있음.
*Chain rule이 특히 나의 연구분야에서 중요한 이유는 CNN은 image→conv→relu→conv→pooling→fc→loss 로 이어지는 거대 합성함수의 덩어리로 이뤄지는데, Backpropagation 시 loss→fc→pooling→conv→conv→image 이러헥 Chain Rule을 적용하기 때문에 중요하다고 볼 수 있음.
   - 추후 신경망에서의 backpropagation의 핵심적인 이론으로 등장

### 8. 편미분과 그라디언트
 · 편미분이 필요한 이유
   - 편미분 = 변수가 여러개 인 다항 함수에서 각각에 대하여 미분하는 것.(=하나의 변수만 움직일 때의 변화율)

   - 손실함수(Loss Function)를 각 parameter(wigth, bias 등)에 대해 미분하여 parameter가 Loss에 미치는 영향을 계산하고 이를 기반으로 optimization을 수행하기 위해 

 · gradient란?
   - 편미분을 통해 계산한 결과값=> 함수의 기울기, 즉 변화율을 여러 변수에 대해 나타낸 벡터이다.

   - 계산한 gradient(편미분)을 통해 parameter를 손실이 감소하는 방향으로 갱신하기 위해 구한다.

### 9. 테일러 급수
 · 어떤 임의의 함수를 다항함수로 나태내는 것.
   - gradient가 왜 가장 가파른 방향을 향하는지 증명할 때 사용함.


### 10. 스칼라를 벡터로 미분
 · 스칼라를 벡터로 미분하는 이유
   - 최적화에서 변수의 변화 방향과 크기를 알기 위하여 스칼라를 벡터로 미분함.

   - Loss는 하나의 숫자로 존재할 것이고, weight는 많으면 수백만개 일 수 있음(w=[w1,w2,...,wn]). 따라서 Loss를 각 Weight에 대해 한 번에 미분해야 하는 ∂L/∂w 가 되는데 이게바로 Optimizer의 입력이 되기 때문.

## 26.08.06. TIL Check List
① 편미분이 왜 필요할까?
ex) f(x,y)=x^2+y^2 같이 변수가 여러 개인 함수에서 왜 일반 미분이 아니라 편미분을 하는지
- (나의 답)하나의 함수의 변수가 2개가 존재하는 이항함수임. 따라서 각 변수에 대한 편미분을 해주어야 x, y에 대한 미분값을 각각 구할 수 있으므로 편미분을 시행함.

② Gradient란 무엇인가? 여기서는 "기울기"라고만 답하면 50점이다.
왜 Gradient가 가장 가파르게 증가하는 방향인지 설명해보자.
- (나의 답)gradient는 함수의 기울기로써 여러변수에 대해 변화율을 나타낸 벡터라고 설명할 수 있음. 계산된 gradient을 통해 parameter를 손실이 감소하는 방향으로 조정함

③ Taylor Series 여기가 제일 어렵다.
완벽히 이해 못 했어도 된다. 다만 강의를 듣고 왜 Taylor Series를 설명했는지 느낌이라도 말해줘.
- (나의 답)사실 여기는 거의 이해하지 못했고 그냥 커다란 방향만 기억하는데 대충 gradient가 왜 가장 가파른 방향을 향하는지 증명하는 내용으로 기억함.

④ 스칼라 → 벡터 미분 여기가 앞으로 논문에서 엄청 많이 나온다.
예를 들어 Loss가 L, Weight가 w=[w1,w2,w3,...] 이면 우리가 구하는 것은 ∂L/∂w 이다.
강의를 듣고 왜 스칼라를 벡터로 미분하는지 이유를 설명해봐.
- (나의 답) 각각의 w에 대한 편미분을 해줘도 되지만, 계산이 복잡해지고 스칼라-벡터 미분을 통해 벡터-gradient간 내적으로 전개가 가능하여 각 변화량으로 적용이 가능하므로 사용함.

- (모범답안) Loss는 하나의 숫자로 존재할 것이고, weight는 많으면 수백만개 일 수 있음(w=[w1,w2,...,wn]). 따라서 Loss를 각 Weight에 대해 한 번에 미분해야 하는 ∂L/∂w 가 되는데 이게바로 Optimizer의 입력이 되기 때문.


새롭게 알게 된 것
 - chain rule과 편미분, gradient, 스칼라-벡터 미분간 모두 서로 이어져 있고 밀접하게 연계가 되어있다는 점을 알게되었다.

이해 안 되는 것
 - 오늘의 study는 모두 정확하게 전부 얘기하라 하면 자신이 없지만 대략적인 개념은 얼추 이해한것 같다.
특히 테일러 급수쪽은 대부분 이해가 되지는 않지만, 해당 개념은 deep learning 신경망의 내부 계산에 있어 필수적으로 등장하는 내용은 아니고, gradient와 parameter간 관계에 있어 등장하는 증명으로 알고있어 더 깊게 들어가진 않았음.

논문과 연결될 것 같은 부분
 - 지금부터는 신경망에서의 계산에 핵심적인 개념들이 전부 등장하여 앞으로 항상 자주 쓰일 개념으로 생각된다. 
 - 수식에 대한 정리
 ∇x​L(x,y) - Gradient | ∂L/∂w - 스칼라→벡터미분 | ∂L/∂w - FGSM Attack


## 📚26.08.07 오늘의 TIL
### 11. 왜 그라디언트는 가장 가파른 방향을 향할까
   - Gradient는 "가장 가파르게 증가하는 방향"을 나타내는 벡터이고, 그 크기(norm)는 그 방향으로의 증가율을 의미한다.
   - 즉, 방향(Direction) : 어디로 가면 가장 빨리 증가하는가? 크기(Magnitude) : 얼마나 빨리 증가하는가? 이 두 가지를 동시에 담고 있기에  Gradient는 단순한 기울기가 아니라 벡터임

### 12. 벡터를 벡터로 미분하는 법
   - 스칼라를 벡터로 미분하는것과 거의 동일하지만 최종식에서 뒷쪽열벡터가 행렬이 됨.  Backpropagation에서 여러 뉴런을 한 번에 계산하기 위해 벡터→벡터 미분을 사용한다.
    * 벡터 미분은 계산을 행렬 연산으로 바꾸기 위한 언어이다.

## 📚26.08.10 오늘의 TIL
-55, 56라인 전치에 대한 개념 추가함

### 13. 벡터를 벡터로 미분할 때의 연쇄 법칙
 · 벡터를 벡터로 미분하면 왜 결과가 행렬 형태가 되는지
   - 각 입력 벡터 요소가 각 출력 벡터 요소에 어떠한 영향을 주는지 따로 계산하기 때문에 결과가 행렬형태가 됌
   * ex) 입력이 3개(x, y, z), 출력이 2개(u, v)라면 | x가 u에 얼마나 영향을 주는지, x가 v에 얼마나 영향을 주는지, |
y가 u에 얼마나 영향을 주는지, y가 v에 얼마나 영향을 주는지,|
z가 u에 얼마나 영향을 주는지, z가 v에 얼마나 영향을 주는지 | 이렇게 모두 따로 계산해서 표(행렬)로 정리


 · Jacobian
   - 입력의 한 요소를 살짝 바꿧을 때, 출력의 한 요소가 얼마다 변하는지를 나타낸 미분값이 들어있는 행렬 = 야코비안 행렬
   - 위 벡터를 벡터로 미분했을 때 나오는 표(행렬)
**  이미지 입력을 $x \in \mathbb{R}^{H \times W \times C}$ 라고 하면, 신경망 f(x) 의 출력에 대한 입력의 Jacobian은 입력의 작은 변화가 출력에 어떤 영향을 주는지 나타낸다.
각 기호의 수학적 의미$x$: 대상 변수 (주로 인공지능/영상처리에서 이미지 테두리 데이터를 의미)$\in$: 오른쪽 집합의 원소(Element)라는 의미$\mathbb{R}$: 실수의 집합 (Real numbers)$H \times W \times C$: 데이터의 차원 크기$H$ (Height): 세로 길이 (행의 개수)$W$ (Width): 가로 길이 (열의 개수)$C$ (Channel): 채널 수 (예: RGB 3채널)
Adversarial attack은 바로 이 민감도를 이용한다.   

### 16. 랜덤 변수와 확률 분포
 · 확률변수
   - 실제 어떠한 사건의 확률적인 결과를 숫자로 대응시키는 함수.(ex 동전을 던졌을때 앞면을 '0' 뒷면을 '1')

 · 확률분포
   - 확률변수가 각 값을 가질 확률이 얼마나 되는지 정리한 분포도
   - 확률분포는 확률질량함수(PMF)에서는 합이 '1' / 확률밀도함수(PDF)에서는 적분값이 '1' 

 · 확률변수와 확률분포의 차이점
   - 확률변수는 어떠한 사건의 결과이고, 확률분포는 그 사건의 결과값을 정리해놓은 느낌으로 볼 수 있음

### 17. 평균과 분산
 · 평균
   - 평균의 종류는 1. 산술평균 2. 기하 평균 3. 조화 평균이 있는데 Deep learning에서는 산술평균의 기댓값(Expectation)을 주로 다룸
   - 기댓값의 정의는 $$E[X] = \sum_{i} x_i P(X = x_i)$$ ($x_i$: 확률변수 $X$가 가질 수 있는 값, $P(X = x_i)$: $x_i$가 나타날 확률)

 · 분산
   - 평균으로부터 퍼진 정도를 나타내는 방법
   - 평균과의 차이의 제곱(편차의 제곱)의 평균으로 나타냄
   - 이산확률변수 : $$V(X) = \sum_{i} (x_i - \mu)^2 Pi​$$
   - 연속확률변수 : $$V(X) = \int_{-\infty}^{\infty} (x - \mu)^2 p(x) \, dx$$


### 18. 균등 분포와 정규 분포
 · 균등 분포
   - 균등 분포 → 생김새가 균등하게 평평한 분포(즉 주사위, 동전 면 등 각각 사건의 확률이 균등한 분포도 = 합은 '1'이 됨)
   - 평균: 1/2(a+b)   (a:시작값, b:끝값)
   - 분산: 1/12(b-a) $$^2$$ 

 · 정규 분포
   - 정규분포 → 평균을 중심으로 대칭적인 종 모양의 확률분포.
   - 평균: $\mu$
   - 분산: $\sigma^2$

 
## 📚26.08.11 오늘의 TIL
최근 study에 대한 내용 복습 및 핵심추리기
 
 #### ① 확률변수란?
   - 실제 어떠한 사건의 확률적인 결과를 숫자로 대응시키는 함수.

 #### ② 확률분포란?
   - 확률변수가 가질 수 있는 모든값과 이러한 값이 나타날 확률을 정리한 함수

 #### ③ 기대값이란?
   - 확률변수가 가져올 수 있는 모든 값들의 확률 가중평균
   - 표본을 실제로 뽑아서 구한 '산술평균'과 달리, 확률모델 전체 집단에 대한 평균
   - 딥러닝에서의 핵심 활용:손실 함수(Loss Function) 최적화: 딥러닝 모델 학습의 본질은 데이터 분포 $p(x, y)$ 전체에 대한 손실의 기대값을 최소화하는 파라미터 $\theta$를 찾는 과정. 
   
 #### ④ 분산이란?
   - 확률변수의 값들이 기대값(평균)으로부터 얼마나 널리 퍼져 있는지를 나타내는 척도
   - 데이터의 가변성이나 불확실성(Uncertainty)의 정도를 측정
   - $\text{Var}(X)$ 또는 $\sigma^2$으로 표기하며, 분산에 제곱근을 취한 값이 표준편차(Standard Deviation, $\sigma$)
   - 수학적 정의: $$\text{Var}(X) = \mathbb{E}[(X - \mathbb{E}[X])^2] = \mathbb{E}[X^2] - (\mathbb{E}[X])^2$$
   - 필터링 모델 개발에서의 중요성 : 모델 불확실성(Uncertainty) 기반 탐지로 드롭아웃(Dropout)을 켠 상태에서 하나의 입력 $x$에 대해 여러 번 추론(Monte Carlo Dropout)을 수행할 때, 정상 입력은 예측 확률의 분산이 매우 작지만, 적대적 예제는 추론할 때마다 결과가 요동쳐 출력 분산이 매우 크게 나타나는 경향이 있음. 
   
   이를 이용해 적대적 공격을 필터링할 수 있음

   (AI첨삭 - Monte Carlo Dropout을 이용한 uncertainty estimation 자체는 가능한 접근이지만, "정상 = 낮은 분산, adversarial = 높은 분산" 이 관계가 항상 성립한다고 가정하면 안 된다.

   공격 종류, 모델 구조, dropout rate, 데이터셋, attack strength 등에 따라 달라질 수 있다. 따라서 지금은 이렇게 기록하는 게 정확하다.
   "Monte Carlo Dropout을 이용한 predictive uncertainty가 adversarial example 탐지에 유용한지 검증해볼 수 있다." 이렇게 바꾸면 연구 아이디어가 된다.
   그리고 재미있는 점은 이게 네가 처음부터 말했던 "공격을 사전에 방지하는 필터 모델" 이라는 연구 방향과 실제로 연결될 수 있다는 것이다.
   다만 아직 논문 주제로 확정하면 안 된다. 나중에 관련 논문을 조사하고 baseline을 확인한 뒤 판단해야 한다.)

 #### ⑤ Jacobian이란?
   - 여러 개의 입력을 받아 여러 개의 출력을 내놓는 다변수 벡터 함수에서, 모든 출력의 모든 입력에 대한 1차 편도함수를 행렬로 모아놓은 것

 #### ⑥ + 왜 Adversarial Attack에서 입력 x에 대한 모델의 Gradient/Jacobian이 중요할까?
   - 입력 $x$에 대한 Gradient/Jacobian은 모델의 예측을 오작동시키기 위해 "입력 픽셀을 어느 방향으로 얼마만큼 미세하게 변형해야 손실이 가장 극대화되는지" 알려주는 지형도(Map)이자 민감도 지표이기 때문에
    
   - Gradient와 Jacobian의 차이:Gradient ($\nabla_{\mathbf{x}} f$): 출력이 단 하나의 스칼라값(예: Loss값 1개)일 때의 미분 벡터($1 \times n$)

   - Jacobian ($J$): 출력이 여러 개로 이루어진 벡터(예: Softmax를 거친 $m$개 클래스별 확률 값)일 때의 미분 행렬. ($m \times n$)


## 📚26.08.12 오늘의 TIL

### 19. 최대 우도 추정(MLE)
지금까지 배운 내용 연결  
<p align="center">· 확률변수  </p>
   <p align="center">↓  </p>
<p align="center">· 확률분포  </p>
   <p align="center">↓  </p>
<p align="center">· 기대값 / 분산  </p>
   <p align="center">↓  </p>
<p align="center">· 정규분포 등 확률모델  </p>
   <p align="center">↓  </p>
<p align="center">· 관측된 데이터가 등장  </p>
   <p align="center">↓  </p>
<p align="center">· "이 데이터를 가장 잘 설명하는 모델은?"  </p>
   <p align="center">↓  </p>
<p align="center">· MLE  </p>

#### ① MLE를 한 문장으로 설명
Maximum Likelihood Estimation이 무엇인가?

  - MLE: 관측된 데이터가 가장 높은 확률로 발생하도록 만드는 모델의 파라미터를 찾는 방법
  * 관측 데이터 D는 이미 존재 → 여러 후보 θ를 넣어봄 → "어떤 θ일 때 지금 데이터를 가장 잘 설명하지?" → 그 θ를 선택  
  **  예를 들어 정규분포의 평균 μ를 찾는다면,  
  관측 데이터: 3, 4, 5, 6, 7 | μ = 3? μ = 5? μ = 10? 각각의 μ에 대해 데이터가 나타날 가능도를 계산하고, 가장 큰 Likelihood를 만드는 μ를 찾는 것이다.


#### ② Probability와 Likelihood의 차이
이게 오늘 가장 중요
예를 들어, P(D∣θ) 에서 데이터를 고정하면? θ를 고정하면? 각각 무엇을 의미하는지 설명

   - D고정 시 위에서 설명한 MLE이며, θ를 고정했을 땐 기존 학습했던 probability가 됨. 둘 다 조건부 확률값이지만, 
   likelihood인 MLE는 합이 '1'이 아니기 때문에 엄연히 확률분포라 할 수 없음.


#### ③ 왜 Log를 사용하는가?
왜 $$\max_{\theta} P(D \mid \theta)$$ 를 그대로 계산하지 않고 $$\max_{\theta} \log P(D \mid \theta)$$ 로 바꾸는지

  - log의 성질로 인해 $$\hat{x} = \arg\max_{x} p(z_1 \mid x) p(z_2 \mid x) = \frac{z_1 + z_2}{2}$$ 에 대한 각각의 식의 각 항의 log를 취해 덧셈으로 바꿔줄 수 있음. (또한, log를 취했을 때 MSE와 연결됨) 

  - Underflow를 방지 (ex. 0.001×0.002×0.0001×⋯ 처럼 계속 곱해지면 값이 엄청 작아져 컴퓨터 계산에서 문제가 생길 수 있으나, Log를 취하면 안정적이다. )


#### ④ MLE와 딥러닝의 연결
MLE가 우리가 사용하는 Loss Function과 어떤 관계가 있을 것 같은가?

  - 딥러닝에서는 일반적으로:  
  $$\mathrm{MLE} = \arg\max_{\theta} P(D \mid \theta)$$ 인 Likelihood를 최대화하고 싶다.  
   그런데 딥러닝은 보통 Loss를 최소화한다.

  그래서: $$\max_{\theta} P(D \mid \theta)$$ 에서  
  → Log를 취하면 $$\max_{\theta} \log P(D \mid \theta)$$  
  → 부호를 바꾸면 $$\min_{\theta} -\log P(D \mid \theta)$$  
  즉, Negative Log-Likelihood(NLL)가 됨.  

   ** 그리고 우리가 사용하는 여러 Loss는 결국 어떤 확률분포를 가정했느냐에 따라 NLL에서 나온다.  
   ** Classification의 경우 MLE → NLL → Cross Entropy  
   ** Regression의 경우 MLE → Gaussian assumption → NLL → MSE  


∴ 현재 우리가 관측한 데이터를 가장 그럴듯하게 만들어 주는 파라미터 θ는 무엇인가?
 → 이를 수식으로 쓰면 $$\theta^{*} = \arg\max_{\theta} P(D \mid \theta)$$ , 여기서 D는 관측한 데이터다.
 →→ 즉 Likelihood를 가장 크게 만드는 파라미터를 찾는 것이 최대우도추정


 · 벡터를 벡터로 미분하면 왜 결과가 행렬 형태가 되는지
   - 각 입력 벡터 요소가 각 출력 벡터 요소에 어떠한 영향을 주는지 따로 계산하기 때문에 결과가 행렬형태가 됌
   * ex) 입력이 3개(x, y, z), 출력이 2  

· MLE에서 가우시안 노이즈를 더하는 이유
 - MLE에서 가우시안 노이즈를 더하는 이유는 모델이 실제 데이터의 불확실성과 오차를 반영하도록 하기 위함.  
가우시안 노이즈를 더하면 데이터가 완벽하게 설명되지 않는 현실적인 상황을 수학적으로 모델링할 수 있고, 이렇게 하면 MLE로 추정한 파라미터가 실제 데이터의 분포와 더 잘 맞게 되고, 오버피팅을 방지하는 효과도 있음.  
딥러닝에서는 예측값과 실제값의 차이를 가우시안 분포로 가정해 MSE 손실함수를 유도할 때도 사용됨.  

· MLE에서 가우시안 노이즈를 추가하는 것이 왜 오버피팅 방지에 도움이 되는지
 - 가우시안 노이즈를 추가하면 모델이 데이터의 작은 변동이나 잡음을 과도하게 학습하지 않게 되어 오버피팅을 방지할 수 있습니다.  
가우시안 노이즈를 추가하면 모델이 완벽하게 모든 데이터를 맞추려 하지 않고, 데이터의 일반적인 패턴만 학습하게 됩니다.  
이 과정에서 노이즈는 실제 데이터에 존재하는 우연적 변동이나 측정 오차를 모방하므로, 모델이 불필요하게 복잡해지는 것을 막아줍니다.  
결국, 노이즈를 고려한 MLE는 파라미터가 데이터의 본질적인 구조에 집중하도록 유도해, 새로운 데이터에 대한 일반화 성능이 높아집니다.  
딥러닝에서는 입력이나 은닉층에 노이즈를 추가하는 방식(예: Dropout, Gaussian noise layer)도 오버피팅 방지에 널리 활용됩니다.  


#### 향후 MLE와의 연결고리 정리 
  <p align="center">MLE</p>
  <p align="center">↓</p>
  <p align="center">Negative Log Likelihood</p>
  <p align="center">↓</p>
  <p align="center">Cross Entropy Loss</p>
  <p align="center">↓</p>
  <p align="center">$$\nabla_{\theta} \mathcal{L}$$</p>
  <p align="center">↓</p>
  <p align="center">Backpropagation</p>
  <p align="center">↓</p>
  <p align="center">$$\nabla_{x} \mathcal{L}$$</p>
  <p align="center">↓</p>
  <p align="center">FGSM / PGD</p>  


  ## 📚26.08.14 오늘의 TIL
  
### 20. 최대 사후 확률(MAP)
 · MAP의 개념
  - Maximum A Posteriori의 줄임말로 데이터와 사전 확률을 모두 고려해 parameter를 추정하고, 베이즈 정리를 기반으로 함.  
  - MLE와 달리 사전분포(prior)를 반영하여 더 현실적인 추정이 가능하도록 함.  
  - 수식:  $\theta^{\text{MAP}} = \arg\max_{\theta} P(\theta \mid X)$  
    즉, 데이터 X가 주어졌을 때 파라미터 θ의 사후 확률이 최대가 되는 값을 찾음  
  - 딥러닝에서는 parameter의 사전분포를 반영하여 최적의 가중치를 찾는데 쓰임.  
  - L2 정규화는 가중치에 대한 정규분포(prior)를 가정한 MAP추정과 동일한 효과를 냄.  
  - 이처럼 MAP은 오버피팅 방지와 더 일반화도니 모델 학습에 기여함.  

 · MLE와 MAP의 비교  
  - ##### MLE
    $$\hat{\theta}_{\text{MLE}}=\arg\max_{\theta}P(X \mid \theta)$$  , 의미: 현재 관측한 데이터 X를 가장 잘 설명하는 θ를 찾는다.  

  - ##### MAP
    MAP : $$\hat{\theta}_{\text{MAP}} = \operatorname*{argmax}_{\theta} P(\theta \mid X)$$ 에서
    베이즈 정리를 적용하면, $$P(\theta \mid X) = \frac{P(X \mid \theta)P(\theta)}{P(X)}$$  
    여기서 P(X)는 θ에 대해 상수이므로,
    $$\hat{\theta}_{\text{MAP}} = \operatorname*{argmax}_{\theta} P(X \mid \theta)P(\theta)$$  



### 21. 정보 이론 기초(Entropy, Cross Entropy, KL-divergence)
  - Entropy: 불확실성의 척도  
  - Cross-Entropy: 두 분포 간 차이(실제 분포 P를 기준으로 할 때, 모델 분포 Q를 사용하여 데이터를 표현하는 데 필요한 평균 정보량)  
    수식은: $$H(P,Q)=-\sum_x P(x)\log Q(x)$$ , 딥러닝에서는 보통: P: 실제 정답 분포 Q: 모델이 예측한 확률분포 가 되며, 그래서 모델의 예측이 실제 정답과 멀어질수록 Cross-Entropy Loss가 커진다.  
  - KL발산: 한 분포가 다른 분포와 얼마나 다른지  
  위의 것들은 딥러닝에서 주로 손실함수로 활용되어 모델의 예측 성능을 평가하고 최적화 함.


#### 지금까지의 흐름 정리  

$$\text{Function}\rightarrow\text{Derivative}\rightarrow\text{Chain Rule}\rightarrow\text{Gradient}\rightarrow\text{Loss}\rightarrow\text{Optimization}$$

함수(Function)는 입력을 출력으로 변환하고,  
미분(Derivative)은 함수의 변화율을 측정,  
연쇄법칙(Chain Rule)은 여러 함수가 합성된 경우 미분을 계산하는 방법이고,  
그라디언트(Gradient)는 다변수 함수에서 가장 빠르게 증가하는 방향을 나타내며,  
손실(Loss)은 모델 예측과 실제 값의 차이를 수치로 표현하며,  
최적화(Optimization)는 손실을 최소화하는 파라미터를 찾는 과정.  
이 흐름이 딥러닝 모델의 학습을 가능하게 함.

- 딥러닝 모델은 학습 데이터에서 정의된 목적함수(Loss)를 최소화하도록 파라미터를 조정하며, 그 결과 실제 정답에 대한 예측 성능과 일반화 성능을 높이는 것을 목표